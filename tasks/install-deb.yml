---
- name: Install dependencies
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - procps
    state: present
    update_cache: true
  become: true

- name: Add Netbird repository
  ansible.builtin.deb822_repository:
    name: netbird
    types: deb
    uris: https://pkgs.netbird.io/debian
    suites: stable
    components: main
    signed_by: https://pkgs.netbird.io/debian/public.key
    state: present
    install_python_debian: true
  register: netbird_repo_added
  become: true

- name: Update apt cache (netbird)
  ansible.builtin.apt:
    update_cache: true
  become: true
  when: >
    netbird_state == 'latest' or
    netbird_repo_added.changed

- name: Install Netbird
  ansible.builtin.apt:
    name: netbird
    state: "{{ netbird_state }}"
  notify: restart netbird
  become: true
